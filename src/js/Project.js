class Project {
    constructor (
        name,
        desc,
        tools,
        langs,
    ) {
        this.name = name;
        this.desc = desc;
        this.tools = tools;
        this.langs = langs;
    }
}

export default Project;