class PortfolioSection {
    constructor (
        name,
        id,
    ) {
        this.name = name;
        this.id = id;
    }
    markup() {
        return `
            <section></section>
        `;
    }
}

export default PortfolioSection;